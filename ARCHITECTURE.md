# Rayvolution Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              Next.js Frontend (Port 3000)                 │   │
│  │  ┌────────┬────────┬──────────┬──────────┬──────────┐    │   │
│  │  │ Login  │Dashboard│Challenges│Marketplace│  Admin  │    │   │
│  │  └────────┴────────┴──────────┴──────────┴──────────┘    │   │
│  │                         ↓                                  │   │
│  │              API Service Layer (lib/api.ts)               │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTP/HTTPS
                             │ (fetch requests)
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                    Express Backend (Port 5000)                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    Middleware Layer                       │   │
│  │  ┌──────────┬────────────┬─────────────┬──────────────┐  │   │
│  │  │  CORS    │   Helmet   │Rate Limiter │  Auth Check  │  │   │
│  │  └──────────┴────────────┴─────────────┴──────────────┘  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                             ↓                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                     Route Layer                           │   │
│  │  /api/auth    /api/users    /api/energy    /api/stats    │   │
│  │  /api/challenges  /api/marketplace  /api/reports         │   │
│  │  /api/admin   /api/chat                                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                             ↓                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                   Controller Layer                        │   │
│  │  Business Logic, Validation, Data Processing              │   │
│  └──────────────────────────────────────────────────────────┘   │
│                             ↓                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                     Model Layer                           │   │
│  │  User  Energy  CityData  Challenge  Marketplace Report   │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │ Mongoose ODM
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                    MongoDB Database                              │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Collections:                                             │   │
│  │  • users          • energies       • citydatas           │   │
│  │  • challenges     • userchallenges • marketplacelistings │   │
│  │  • transactions   • reports        • chatmessages        │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                             ↑
                             │ (External APIs)
                    ┌────────┴─────────┐
            ┌───────▼──────┐   ┌──────▼────────┐
            │ OpenWeather  │   │  Groq AI API  │
            │     API      │   │   (Chatbot)   │
            └──────────────┘   └───────────────┘
```

---

## Request Flow Example: User Login

```
1. USER enters email & password
   ↓
2. FRONTEND sends POST to http://localhost:5000/api/auth/login
   {
     email: "user@example.com",
     password: "password123"
   }
   ↓
3. EXPRESS receives request
   ↓
4. MIDDLEWARE checks:
   - CORS (✓ allowed origin)
   - Rate limit (✓ under 100 req/15min)
   - Input validation (✓ email & password present)
   ↓
5. ROUTE /api/auth/login → auth.controller.login()
   ↓
6. CONTROLLER:
   - Finds user in database
   - Compares password with bcrypt
   - Generates JWT token
   - Updates user streak
   ↓
7. DATABASE queries & updates
   ↓
8. RESPONSE sent back to frontend:
   {
     success: true,
     data: {
       user: { ... },
       accessToken: "eyJhbGc...",
       refreshToken: "eyJhbGc..."
     }
   }
   ↓
9. FRONTEND stores token & user data
   ↓
10. USER redirected to /dashboard
```

---

## Authentication Flow

```
┌──────────┐
│  Signup  │
└────┬─────┘
     │ POST /api/auth/signup
     │ { name, email, password, city }
     ↓
┌────────────────────────┐
│  Backend validates     │
│  - Email unique?       │
│  - Password length?    │
│  - City valid?         │
└────┬───────────────────┘
     │ ✓ Valid
     ↓
┌────────────────────────┐
│  Hash password         │
│  (bcrypt, 10 rounds)   │
└────┬───────────────────┘
     │
     ↓
┌────────────────────────┐
│  Create user in DB     │
│  Initial stats:        │
│  - greenCoins: 100     │
│  - streak: 0           │
└────┬───────────────────┘
     │
     ↓
┌────────────────────────┐
│  Generate tokens:      │
│  - JWT access token    │
│  - JWT refresh token   │
└────┬───────────────────┘
     │
     ↓
┌────────────────────────┐
│  Set HTTP-only cookie  │
│  Return user + tokens  │
└────┬───────────────────┘
     │
     ↓
┌──────────┐
│  Login   │ → Same flow but:
└──────────┘   - Check password
               - Update streak
               - Award streak bonuses
```

---

## Database Schema Relationships

```
┌─────────────────┐
│      User       │
│ _id (ObjectId)  │◄──────┐
│ email           │       │
│ password        │       │
│ stats {...}     │       │
│ achievements[]  │       │
└─────────────────┘       │
         ▲                │
         │                │
         │ userId         │ userId
         │                │
    ┌────┴────────┐  ┌────┴──────────┐
    │   Energy    │  │  UserChallenge│
    │ userId ─────┤  │  userId ───────┤
    │ city        │  │  challengeId ──┼──┐
    │ type        │  │  progress      │  │
    │ amount      │  │  completed     │  │
    └─────────────┘  └────────────────┘  │
                                         │
         ▲                               │
         │                               │
         │ sellerId/buyerId              │ challengeId
         │                               │
    ┌────┴─────────────┐      ┌─────────▼──────┐
    │MarketplaceListing│      │   Challenge    │
    │ sellerId ────────┤      │ _id (ObjectId) │
    │ buyerId ─────────┤      │ title          │
    │ energyTokens     │      │ type           │
    │ status           │      │ reward {...}   │
    └──────────────────┘      └────────────────┘
         │
         │ listingId
         │
    ┌────▼─────────┐
    │ Transaction  │
    │ fromUserId ──┤──┐
    │ toUserId ────┤──┼──► User
    │ listingId    │  │
    │ energyTokens │  │
    └──────────────┘  │
                      │
         ▲            │
         │            │
         │ userId     │
         │            │
    ┌────┴──────┐    │
    │  Report   │    │
    │ userId ───┼────┘
    │ type      │
    │ status    │
    │ upvotes[] │──┐
    │ comments[]│  │
    └───────────┘  │
                   │ (array of userIds)
                   └──► User
```

---

## Feature Flow: Recording Solar Energy

```
1. USER enters "50 kWh saved today"
   ↓
2. FRONTEND:
   api.energy.record({
     city: "Karachi",
     type: "savings",
     amount: 50,
     source: "solar"
   })
   ↓
3. BACKEND receives POST /api/energy
   ↓
4. MIDDLEWARE:
   ✓ User authenticated (JWT valid)
   ✓ Input validated (amount > 0)
   ↓
5. CONTROLLER creates Energy record
   ↓
6. DATABASE: Insert into energies collection
   ↓
7. CONTROLLER updates User stats:
   - energySaved += 50
   - co2Reduced += 25 (50 * 0.5)
   - energyTokens += 50
   - greenCoins += 100 (50 * 2)
   - Calculate new sustainabilityScore
   ↓
8. DATABASE: Update user document
   ↓
9. CONTROLLER updates CityData:
   - Karachi.totalEnergySaved += 50
   - Karachi.totalCO2Reduced += 25
   ↓
10. DATABASE: Update citydata document
    ↓
11. RESPONSE sent back:
    {
      success: true,
      data: {
        energyRecord: {...},
        stats: { updated user stats }
      }
    }
    ↓
12. FRONTEND updates UI:
    - Show success message
    - Update displayed stats
    - Maybe confetti animation! 🎉
```

---

## Security Layers

```
┌────────────────────────────────────────┐
│        Client (Browser)                │
│  - Can't modify server code            │
│  - Token stored in localStorage/cookie │
└──────────────┬─────────────────────────┘
               │ HTTPS (Production)
               ↓
┌────────────────────────────────────────┐
│         Layer 1: Network               │
│  - CORS (only allowed origins)         │
│  - Rate Limiting (100 req/15min)       │
│  - Helmet.js (security headers)        │
└──────────────┬─────────────────────────┘
               ↓
┌────────────────────────────────────────┐
│         Layer 2: Authentication        │
│  - JWT signature verification          │
│  - Token expiration check              │
│  - User existence check                │
└──────────────┬─────────────────────────┘
               ↓
┌────────────────────────────────────────┐
│         Layer 3: Authorization         │
│  - Role check (user/admin)             │
│  - Resource ownership check            │
│  - Permission validation               │
└──────────────┬─────────────────────────┘
               ↓
┌────────────────────────────────────────┐
│         Layer 4: Input Validation      │
│  - express-validator rules             │
│  - Mongoose schema validation          │
│  - SQL injection prevention            │
└──────────────┬─────────────────────────┘
               ↓
┌────────────────────────────────────────┐
│         Layer 5: Data Access           │
│  - MongoDB user-specific queries       │
│  - Automatic field filtering           │
│  - Password exclusion (select: false)  │
└────────────────────────────────────────┘
```

---

## Deployment Architecture (Production)

```
┌──────────────────────────────────────────────────────────┐
│                    Internet Users                         │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ↓
┌──────────────────────────────────────────────────────────┐
│                 CDN (Cloudflare/Vercel)                   │
│  - Static assets caching                                 │
│  - DDoS protection                                        │
└────────────────────┬─────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ↓                         ↓
┌────────────────┐      ┌──────────────────┐
│  Frontend      │      │    Backend       │
│  (Vercel)      │      │ (Railway/Heroku) │
│  Next.js       │◄────►│  Express.js      │
│  Port 443      │      │  Port 443        │
└────────────────┘      └────────┬─────────┘
                                 │
                                 ↓
                    ┌─────────────────────┐
                    │   MongoDB Atlas     │
                    │   (Cloud Database)  │
                    │   - Automatic backup│
                    │   - Replica sets    │
                    │   - Monitoring      │
                    └─────────────────────┘
```

---

## Data Flow: Real-time Leaderboard Update

```
Multiple Users Saving Energy Simultaneously:

User A saves 30 kWh    User B saves 45 kWh    User C saves 20 kWh
       │                      │                       │
       ↓                      ↓                       ↓
   ┌───────────────────────────────────────────────────┐
   │           Backend processes in parallel           │
   │  Thread 1: Update A    Thread 2: Update B        │
   │  Thread 3: Update C                               │
   └───────────────┬───────────────────────────────────┘
                   │
                   ↓
   ┌───────────────────────────────────────────────────┐
   │         MongoDB handles concurrent writes          │
   │  - Atomic operations ensure consistency            │
   │  - Each user gets correct updated stats            │
   └───────────────┬───────────────────────────────────┘
                   │
                   ↓
   ┌───────────────────────────────────────────────────┐
   │      Leaderboard query aggregates all users        │
   │  db.users.find().sort({                            │
   │    'stats.sustainabilityScore': -1                 │
   │  })                                                │
   └───────────────┬───────────────────────────────────┘
                   │
                   ↓
   ┌───────────────────────────────────────────────────┐
   │       All users get latest leaderboard             │
   │  1. User B (45 kWh) - Score: 92                   │
   │  2. User A (30 kWh) - Score: 85                   │
   │  3. User C (20 kWh) - Score: 78                   │
   └───────────────────────────────────────────────────┘
```

---

## Performance Optimization Strategy

```
┌─────────────────────────────────────────┐
│          Frontend Caching               │
│  - React Query (auto-refetch)           │
│  - SWR (stale-while-revalidate)         │
│  - LocalStorage for non-critical data   │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│          Backend Caching                │
│  - Redis (future): Leaderboard cache    │
│  - In-memory: City statistics           │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│       Database Optimization             │
│  - Indexes on frequently queried fields │
│  - Aggregation pipelines                │
│  - Limit query results                  │
│  - Select only needed fields            │
└─────────────────────────────────────────┘

Current Indexes:
- users: email (unique), city, role
- energies: userId + date, city + date
- challenges: isActive, endDate
- reports: userId, status, createdAt
- userchallenges: userId + challengeId (unique)
```

---

## Error Handling Flow

```
Request → Middleware → Controller → Model → Database
  ↓          ↓            ↓          ↓         ↓
Error?    Error?       Error?     Error?   Error?
  ↓          ↓            ↓          ↓         ↓
  └──────────┴────────────┴──────────┴─────────┘
                     │
                     ↓
          ┌──────────────────┐
          │  Error Caught    │
          │  in Express      │
          └────────┬─────────┘
                   │
                   ↓
          ┌─────────────────────────────┐
          │  Global Error Handler       │
          │  (server.js)                │
          │  - Log error                │
          │  - Format response          │
          │  - Hide sensitive details   │
          └────────┬────────────────────┘
                   │
                   ↓
          ┌─────────────────────────────┐
          │  Response to Client         │
          │  {                          │
          │    success: false,          │
          │    message: "User friendly" │
          │  }                          │
          └─────────────────────────────┘
```

---

## Scalability Path

### Current (v1.0)
```
Single Server → MongoDB → Up to 10K users
```

### Future (v2.0)
```
Load Balancer
    ├── Server 1 ──┐
    ├── Server 2 ──┼──→ Redis Cache ──→ MongoDB Cluster
    └── Server 3 ──┘                    (Replica Sets)
```

### Enterprise (v3.0)
```
CDN ──→ Load Balancer ──→ Microservices:
                          ├── Auth Service
                          ├── Energy Service
                          ├── Marketplace Service
                          └── Analytics Service
                                    ↓
                          Message Queue (RabbitMQ)
                                    ↓
                          Multiple Databases:
                          ├── PostgreSQL (Transactions)
                          ├── MongoDB (User Data)
                          ├── Redis (Cache)
                          └── Elasticsearch (Search)
```

---

This architecture is:
- ✅ **Modular** - Easy to maintain & extend
- ✅ **Secure** - Multiple security layers
- ✅ **Scalable** - Can grow with your users
- ✅ **Performant** - Optimized queries & caching
- ✅ **RESTful** - Standard API patterns
- ✅ **Documented** - Clear code & docs

Ready to handle thousands of users saving solar energy across Pakistan! 🌞🇵🇰
